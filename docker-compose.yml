services:
  backend:
    build: .
    env_file: .env
    ports: ["8000:8000"]
  dashboard:
    build: .
    env_file: .env
    command: streamlit run dashboard/streamlit_app.py --server.address 0.0.0.0
    ports: ["8501:8501"]
    depends_on: [backend]
